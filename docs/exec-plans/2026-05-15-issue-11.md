# Issue #11 — [Design] RepeatReservation 화면 + 홈 시트 디폴트

- 원본: https://github.com/When2Go/Mobile_When2Go/issues/11
- Labels: `design`, `ui`
- PRD ID: **F-U03** (반복 예약 자동 Trip 생성)
- 디자인 시안: `../design/src/app/pages/RepeatReservation.tsx`

## 목표

1. 마이페이지에서 진입하는 **반복 예약 관리 화면**을 mock UI로 구현.
   요일/시간/경로 옵션을 정해 반복 Trip을 자동 생성하는 자리. 이 PR에서는 mock 동작(추가/수정/토글/삭제)까지만.
2. **홈 화면 바텀시트**의 초기 상태를 컴팩트(25%)로 변경.
   기본 진입 시 지도 + 자주 가는 곳 carousel만 노출하고, 사용자가 위로 끌어올려야 최근 목적지·광고가 펼쳐지도록.

## 관련 파일

신규:
- `src/types/repeat.types.ts` — `RepeatItem`, `RepeatFormData`
- `src/constants/repeat.ts` — `DAYS`, `REPEAT_ROUTE_OPTIONS`, default time, 빈/안내 문자열
- `src/components/repeat/RepeatReservationCard.tsx`
- `src/components/repeat/RepeatEditModal.tsx`
- `src/components/repeat/EmptyState.tsx`
- `app/repeat.tsx`

수정:
- `src/components/mypage/RepeatReservationLink.tsx` — placeholder 주석 제거 (라우트 실존)
- `app/index.tsx` — `SHEET_INITIAL_INDEX` 1 → 0

## 작업 분해

- [ ] 타입/상수 정의
- [ ] `RepeatReservationCard` — 이름/목적지/요일 dot/도착 시간 + 활성 토글 + 스와이프 삭제
- [ ] `RepeatEditModal` — 이름 인풋 + 출발지/목적지 placeholder + 요일 멀티셀렉트 + 도착 시간 픽커(setup `TimeWheelPicker` 재사용) + 경로 옵션 grid
- [ ] `EmptyState` — Repeat2 아이콘 + 첫 반복 예약 만들기 CTA
- [ ] `app/repeat.tsx` — 리스트/추가/수정 상태, mock 데이터(2건)
- [ ] mypage 진입 링크 placeholder 주석 정리
- [ ] 홈 `SHEET_INITIAL_INDEX = 0`
- [ ] Light/Dark 양쪽 시각 확인

## 완료 기준

- Light/Dark 모두 적용, 하드코딩 색상 0건
- 추가/수정/활성토글/삭제 mock 정상 동작
- 요일 멀티셀렉트 UI가 시안 수준(7개 동일 폭 버튼)
- 홈 진입 시 25% 스냅이 기본
- `npm test` 통과, Hook(ESLint+tsc) 통과

## 범위 밖

- 실제 반복 예약 동기화·자동 Trip 생성 (Route API 이슈)
- 시간 픽커 모달화 (setup 화면 픽커 그대로 사용)

## 의존

- #2(인프라), #6(Setup) 머지 완료 → setup 픽커 재사용 가능
