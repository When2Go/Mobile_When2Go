# exec-plan: issue #62

- 원본 이슈: https://github.com/When2Go/Mobile_When2Go/issues/62
- 제목: [Bug] Home MapPreview에서 커스텀 마커와 현재 위치 점이 어긋남
- Labels: bug
- 작업 유형: bug (UI 영역)
- 브랜치: `fix/issue-mappreview-bluedot#62`

## 배경 / 방향 정정

이슈 본문 완료 기준은 "초록 핀 제거 → SDK 내장 파란 점만 남김"이었으나,
실제 확인 결과 **정확도가 반대**였다.

| 표시 | 좌표 소스 | 정확도 |
|---|---|---|
| 초록 핀 (`NaverMapMarkerOverlay`) | `useCurrentLocation`의 `lat/lng` (expo-location) | 정확 |
| 파란 점 (`Follow` 모드 내장 오버레이) | 네이버 SDK 내부 GPS | 부정확 |

따라서 **버리는 쪽을 반대로** 한다. 부정확한 SDK 내장 파란 점을 제거하고,
정확한 `useCurrentLocation` 좌표를 `locationOverlay` prop으로 직접 지정해 파란 점 모양으로 그린다.
화면 결과는 "파란 점 하나"로 동일하되 좌표 소스가 정확한 쪽이 된다.

## 목표

- 정확한 현재 위치(`useCurrentLocation` 좌표)에 표준 파란 점 하나만 표시
- 줌 변경 시 어긋남 없음 (점·카메라 모두 동일 좌표 소스)
- 권한 미허용 폴백(서울시청 중심 + 캡션) 동작 유지

## 관련 파일

- `src/components/home/MapPreview.tsx` (주 변경)
- `docs/references/naver-maps-api-schema.md` (4.3 위치 추적 섹션 갱신)

## 작업 분해

- [ ] `NaverMapMarkerOverlay` import + JSX 블록 제거
- [ ] `setLocationTrackingMode('Follow'/'None')` 호출 + 관련 `useEffect` 제거
- [ ] `NaverMapView`에 `locationOverlay` prop 추가
      - `isVisible: isGranted`
      - `position: { latitude: lat, longitude: lng }`
      - `image: { symbol: 'blue' }`, `anchor: { x: 0.5, y: 0.5 }`
- [ ] `mapRef`가 더 이상 필요 없으면 정리 (locationOverlay는 prop 제어이므로 ref 불필요할 수 있음)
- [ ] 시뮬레이터(iPhone 16 Pro) 확인: 권한 허용 시 정확 위치 파란 점, 줌 어긋남 없음, 폴백 깨짐 없음
- [ ] `docs/references/naver-maps-api-schema.md` 갱신
- [ ] 이슈 시작/완료 코멘트 (방향 정정 명시)

## 완료 기준

- [ ] 정확한 좌표에 파란 점만 표시
- [ ] 확대/축소 시 점이 어긋나지 않음
- [ ] 권한 미허용 폴백 동작 유지
- [ ] when2go-qa 리뷰 [CRITICAL] 0건
