# exec-plan: issue #77

**이슈**: https://github.com/When2Go/Mobile_When2Go/issues/77
**Labels**: refactor
**PRD ID**: 없음

## 목표

반복 예약 카드 탭 시 수정 모달(edit 모드) 진입을 비활성화합니다.
PUT /reservations/{id} 백엔드 엔드포인트 미구현으로 인해 카드 탭 → 저장 시 서버 반영이 되지 않아 사용자 혼란을 방지합니다.

## 관련 파일

- `app/repeat.tsx` — `RepeatReservationCard`에 `onPress={handleEdit}` 연결 해제
- `src/components/repeat/RepeatReservationCard.tsx` — `onPress` prop optional로 변경

## 작업 체크리스트

- [ ] `app/repeat.tsx`: `RepeatReservationCard`의 `onPress={handleEdit}` 제거
- [ ] `src/components/repeat/RepeatReservationCard.tsx`: interface에서 `onPress`를 optional(`?`)로 변경
- [ ] `RepeatReservationCard`: `handlePress` 콜백에서 `onPress`가 undefined일 때 no-op 처리
- [ ] ESLint + TypeScript 에러 없음 확인

## 완료 기준

- 반복 예약 카드 탭 → 아무 반응 없음
- `+추가` 버튼 → 모달(add 모드) 정상 동작
- `handleEdit`, `editTarget`, `isEditOpen` 상태 제거 금지 (add 모드에서 계속 사용)
- `RepeatEditModal`의 `mode='edit'` 분기 유지
