# Exec Plan — Issue #91

**이슈**: https://github.com/When2Go/Mobile_When2Go/issues/91  
**Labels**: refactor  
**브랜치**: `refactor/issue-remove-autofocus#91`

## 목표

`SearchInput` 컴포넌트 마운트 시 자동으로 키보드가 팝업되는 문제를 수정합니다.  
사용자가 입력창을 직접 탭할 때만 키보드가 올라오도록 자동 포커스 로직을 전부 제거합니다.

## 관련 파일

- `src/components/search/SearchInput.tsx` — 수정 대상
- `app/search.tsx` — SearchInput 사용 화면 (ref 외부 사용 없음 확인)

## 작업 분해

- [x] `src/components/search/SearchInput.tsx`에서 다음 제거:
  - `AUTOFOCUS_DELAY_MS` 상수
  - `useEffect` 블록 전체 (timer + clearTimeout)
  - `useRef<TextInput>` 선언 및 `ref={inputRef}` prop
  - 미사용 import (`useEffect`, `useRef`)

## 완료 기준

- [ ] 검색창 진입 시 키보드가 자동으로 올라오지 않음
- [ ] 사용자가 입력창을 탭했을 때 키보드가 정상적으로 올라옴
- [ ] ESLint + TypeScript 에러 없음
