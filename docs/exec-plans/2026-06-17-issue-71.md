# exec-plan: issue #71 — repeat.tsx API 연동

**이슈**: https://github.com/kangwook-kim02/Mobile_When2Go/issues/71  
**Labels**: feature  
**브랜치**: feat/issue-repeat-api#71

---

## 목표

MOCK_REPEATS 로컬 상태로만 동작하는 반복 예약 화면을 실제 API(GET/POST/PUT/DELETE)와 연동하고,
GET 실패 시 AsyncStorage 로컬 캐시로 폴백한다.

---

## 관련 파일

| 파일 | 역할 |
|------|------|
| `app/repeat.tsx` | 반복 예약 화면 (handleSave, handleDelete 수정) |
| `src/types/repeat.types.ts` | RepeatItem 타입에 reservationId, 좌표 필드 추가 |
| `src/api/reservation/types.ts` | GET/POST/PUT/DELETE 요청·응답 타입 (신규) |
| `src/api/reservation/index.ts` | getReservations, createReservation, updateReservation, deleteReservation 함수 (신규) |
| `src/utils/reservationTransform.ts` | 변환 유틸 6종 (신규) |
| `src/utils/__tests__/reservationTransform.test.ts` | 변환 유틸 단위 테스트 30건 (TDD 먼저) |
| `src/constants/storageKeys.ts` | REPEATS 스토리지 키 추가 |

---

## 작업 분해

- [x] TDD: `src/utils/__tests__/reservationTransform.test.ts` 먼저 작성
- [x] 구현: `src/utils/reservationTransform.ts` (daysToRepeatDays, toArrivalTimeString, routeOptionToApiOption, repeatDaysShortToNumbers, parseArrivalTimeString, routeOptionToApiPutOption)
- [x] `src/api/reservation/types.ts` 생성 (모든 요청·응답 타입)
- [x] `src/api/reservation/index.ts` 생성 (GET/POST/PUT/DELETE)
- [x] `src/types/repeat.types.ts` — reservationId?, originLat?, originLng?, destLat?, destLng? 추가
- [x] `src/constants/storageKeys.ts` — REPEATS 키 추가
- [x] `app/repeat.tsx` — 마운트 시 GET + AsyncStorage 병합, handleSave POST/PUT 연동, handleDelete API 연동

---

## 완료 기준

- 마운트 시 GET /api/reservations 호출 → 서버+캐시 병합 (GET 실패 시 캐시 폴백)
- 반복 예약 추가 시 좌표가 있으면 POST /api/reservations 호출, reservationId 로컬 보관
- 반복 예약 수정 시 reservationId + 좌표 있으면 PUT /api/reservations/{id} 호출
- 반복 예약 삭제 시 reservationId 있으면 DELETE /api/reservations/{id} 호출
- 앱 재시작 후 AsyncStorage에서 목록 복원 (GET 실패 폴백)
- 변환 유틸 단위 테스트 30건 전부 통과

---

## 제약 사항

- handleSelectLocation 현재 no-op stub → 좌표 없이 저장 시 로컬 전용 (API 비호출)
- PUT routeOption enum(OPTIMAL/MIN_TRANSFER/SUBWAY_FIRST/BUS_ONLY)이 POST(DRIVE/WALK/BICYCLE/TRANSIT)와 다름 — routeOptionToApiPutOption으로 분리 매핑
