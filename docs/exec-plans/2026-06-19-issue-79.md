# Exec Plan — Issue #79

**이슈**: https://github.com/kangwook-kim02/Mobile_When2Go/issues/79  
**제목**: [Bug] getReservations() 비배열 응답 시 repeat.tsx 크래시 — serverItems.map is not a function  
**Labels**: bug  
**브랜치**: `fix/issue-reservation-get-array-guard#79`

---

## 원인

`src/api/reservation/index.ts:16`
```ts
return data.data ?? [];
```
`??`는 `null`/`undefined`만 fallback — `{}` 같은 truthy 비배열 객체는 통과하여 `serverItems.map()` 호출 시 TypeError 발생.

---

## 관련 파일

- `src/api/reservation/index.ts` — 1줄 수정 (`??` → `Array.isArray` 가드)
- `src/api/reservation/__tests__/getReservations.test.ts` — 신규 테스트

---

## 작업 분해

- [ ] `getReservations()` 반환 표현식 수정: `Array.isArray(data.data) ? data.data : []`
- [ ] 단위 테스트: 정상 배열 / null / undefined / 빈 객체 / 페이지네이션 객체 응답 케이스

---

## 완료 기준

- [ ] 비배열 응답 시 TypeError 없이 `[]` 반환
- [ ] `npm test` 전체 통과
- [ ] ESLint + TypeScript 에러 없음
