# Exec Plan — Issue #6: Setup 화면 (iOS 칩 픽커 + 경로 옵션 mock)

- **이슈**: https://github.com/When2Go/Mobile_When2Go/issues/6
- **라벨**: design, ui
- **PRD ID**: F-M02 (도착 시간 설정), F-M03 (경로 옵션)
- **브랜치**: `design/issue-setup-screen#6`

## 목표

검색에서 넘어온 목적지를 받아, **도착 시간 + 경로 옵션**을 설정하는 화면을 placeholder → 실 구현으로 교체.
iOS 스타일 칩 헤더(날짜·시간 토글) + 분리 픽커(캘린더 / 오전·오후 휠) + 경로 옵션 카드 + CTA.

## 디자인 시안

- `../design/src/app/pages/Setup.tsx` — 1차 참조 (Vite HTML 시안)

## 관련 파일

| 경로 | 상태 |
|------|------|
| `app/setup.tsx` | 수정 (placeholder → 실 구현, 검색 destination 파라미터 수신) |
| `src/components/setup/DestinationHeader.tsx` | 신규 (목적지 표시 + 변경 버튼) |
| `src/components/setup/ArrivalTimePicker.tsx` | 신규 (오케스트레이터 — 칩 헤더 + 분기) |
| `src/components/setup/ArrivalChipHeader.tsx` | 신규 (날짜/시간 토글 칩) |
| `src/components/setup/TimeWheelPicker.tsx` | 신규 (오전·오후 / 시 / 분 3열 휠) |
| `src/components/setup/DateCalendarPicker.tsx` | 신규 (월 그리드 캘린더) |
| `src/components/setup/RouteOptionList.tsx` | 신규 (지하철+버스 / 지하철만 / 버스만 mock) |
| `src/components/setup/DepartButton.tsx` | 신규 (출발 시간 계산 CTA) |
| `src/constants/setup.ts` | 신규 (라우트 옵션 mock, 시간 상수) |

## 작업 분해

- [x] 브랜치 생성 `design/issue-setup-screen#6`
- [ ] `src/constants/setup.ts` — 라우트 옵션 mock, 시간 옵션(시·분 배열), 시간 상한
- [ ] `DestinationHeader` — MapPin 아이콘 + 목적지 텍스트 + `변경` 버튼 (`/search` 이동)
- [ ] `ArrivalChipHeader` — `날짜 / 시간` 토글 칩 (활성 시 primary 색)
- [ ] `TimeWheelPicker` — 오전·오후 / 시(1~12) / 분(5단위) 3열 휠. mock 정렬(중앙 강조)
- [ ] `DateCalendarPicker` — 월 그리드, 좌우 화살표, 선택일 강조
- [ ] `ArrivalTimePicker` — 칩 헤더 + 두 픽커 분기 + 안전 버퍼 안내 박스
- [ ] `RouteOptionList` — 3개 카드 그리드, 활성 카드 ring·primary 톤
- [ ] `DepartButton` — full-width CTA, 비활성 조건(목적지 또는 시간 미설정)
- [ ] `app/setup.tsx` — Back 헤더 + 위 컴포넌트 조립, `destination` 파라미터 수신, `/result` 이동
- [ ] Light / Dark 모드 토큰 일관성 검증

## 완료 기준

- [ ] Light / Dark 모드 모두 적용 — DESIGN.md 토큰만 사용 (하드코딩 색상 0)
- [ ] 칩 토글 → 날짜 픽커 ↔ 시간 픽커 인스턴트 전환
- [ ] 시간 휠에서 오전·오후 탭으로 토글 가능
- [ ] 캘린더 날짜 탭으로 선택일 변경
- [ ] 경로 옵션 3개 중 단일 선택
- [ ] CTA 탭 시 `/result` 이동
- [ ] 목적지 없음 → CTA 비활성 (opacity)
- [ ] TypeScript 에러 0
- [ ] NativeWind 시맨틱 토큰만 사용

## 작업 범위 밖

- 실 출발 시간 계산 로직 (Trip API 별도 이슈)
- 시간 상한(오후 11시) 검증 로직 — mock UI 안내만 표시
- 휠 픽커의 실제 제스처(스와이프 스크롤) — 추후 별도 이슈
