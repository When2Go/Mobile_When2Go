# Exec Plan — Issue #96

**이슈**: https://github.com/When2Go/Mobile_When2Go/issues/96  
**라벨**: feature  
**브랜치**: feat/issue-reservation-map-depart#96

## 목표

- 예약 확인 모달(`ReservationCompleteModal`)에 선택 경로 지도 미리보기 추가
- 일정 카드(`ReservationCard`)에 [출발하기] 버튼 추가 → 홈 지도에 경로 폴리라인 표시
- 지도 하단 [출발하기] / [취소] 버튼으로 경로 표시 상태 제어

## 관련 파일

| 파일 | 역할 |
|---|---|
| `src/stores/activeTripStore.ts` | 신규 — 출발 모드 전용 상태 |
| `src/types/schedule.types.ts` | encodedPolyline, boardingCoord 필드 추가 |
| `src/components/result/ReservationCompleteModal.tsx` | 지도 미리보기 + 스냅 포인트 조정 |
| `src/components/schedule/ReservationCard.tsx` | [출발하기] 버튼 추가 |
| `src/components/home/MapPreview.tsx` | activeTripStore 읽기 + 오버레이 버튼 |

## 작업 분해

- [x] `activeTripStore` 생성 (`activeTripPolyline`, `activeTripBoardingCoord`, `isActiveTrip`, `hasDeparted`)
- [x] `ScheduleItem` 타입에 `encodedPolyline?`, `boardingCoord?` 추가
- [x] `ReservationCompleteModal` 스냅 포인트 `52%` → `78%` + 지도 미리보기 영역 추가
- [x] `ReservationCard` [출발하기] 버튼 추가 (polyline 없으면 미표시)
- [x] `MapPreview` activeTripStore 연동 + 오버레이 버튼

## 완료 기준

- 예약 확인 모달에서 선택 경로 폴리라인이 지도에 표시됩니다
- `ReservationCard` STATUS 배지 왼쪽에 [출발하기] 버튼이 표시됩니다
- [출발하기] 버튼을 누르면 홈 탭으로 이동하고 해당 경로가 지도에 표시됩니다
- 지도 하단에 [출발하기] / [취소] 버튼 오버레이가 표시됩니다
- [출발하기]는 경로 UI를 유지하고, [취소]는 경로를 지웁니다
- polyline 데이터가 없는 기존 카드는 [출발하기] 버튼 미표시
