# exec-plan: issue #99

- **이슈**: https://github.com/When2Go/Mobile_When2Go/issues/99
- **Labels**: bug
- **PRD ID**: -

## 목표

하단 NavItem을 현재 활성 탭과 동일한 경로에서 눌러도 `router.push()`가 호출되지 않도록 방어 처리하여 불필요한 리렌더링을 제거합니다.

## 관련 파일

- `src/components/common/MobileLayout.tsx` — NavItem onPress 핸들러 수정 대상

## 작업 분해

- [ ] `MobileLayout.tsx` 각 NavItem의 `onPress`에 `pathname === 목표경로` 조건 추가 → 같은 경로이면 push 건너뜀
- [ ] 4개 탭(홈·일정·경로·MY) 모두 적용 확인

## 완료 기준

- 현재 활성 탭을 재클릭해도 화면이 리렌더링(깜빡임)되지 않음
- 다른 탭으로 이동 시 정상 동작
