# Exec Plan — Issue #18

**이슈**: https://github.com/kangwook-kim02/Mobile_When2Go/issues/18  
**제목**: [Feature] Settings 로컬 저장 — 안전 버퍼·알림 설정 AsyncStorage 영속화  
**Labels**: feature, api  
**PRD ID**: F-U01(안전 버퍼), F-U05(알림 설정), F-N05(소리/진동)  
**브랜치**: `feat/issue-settings-notification-mode#18`

---

## 현황 분석

| 항목 | 상태 |
|------|------|
| `settingsStore` — bufferMinutes persist | ✅ 이미 구현됨 |
| `settingsStore` — nickname persist | ✅ 이미 구현됨 |
| `mypage.tsx` — buffer store 연결 | ✅ 이미 구현됨 |
| 알림 방식 — 시스템 설정 위임 | ✅ 의도된 동작 (in-app 선택 불필요) |
| 테스트 — store 액션 (clamp·nickname) | ✅ 이미 존재 |
| 테스트 — persist/rehydration 동작 | ❌ 없음 |

---

## 목표

persist 미들웨어가 실제로 AsyncStorage에 쓰고, 앱 재시작 시 복원하는 동작을 단위 테스트로 검증합니다.

---

## 관련 파일

- `src/stores/__tests__/settingsStore.test.ts` — persist 동작 테스트 추가 (유일 변경)

---

## 작업 분해

- [ ] **[TDD]** persist write 테스트: `setBufferMinutes` 호출 후 `AsyncStorage.setItem`이 정확한 키·값으로 호출됐는지 확인
- [ ] **[TDD]** rehydration 테스트: AsyncStorage에 저장된 값으로 store를 재초기화하면 값이 복원되는지 확인
- [ ] **[TDD]** partialize 테스트: persist 대상 필드(bufferMinutes, nickname)만 저장되는지 확인

---

## 완료 기준

- [ ] `npm test` 전체 통과
- [ ] persist write / rehydration / partialize 3가지 시나리오 커버
- [ ] ESLint + TypeScript 에러 없음
