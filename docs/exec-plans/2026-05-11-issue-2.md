# issue #2 — 디자인 토큰·공통 컴포넌트 셋업

- 원본: https://github.com/When2Go/Mobile_When2Go/issues/2
- Labels: chore, ui, infra
- PRD ID: (없음 — infra 작업)
- 브랜치: `feat/issue-design-system-infra#2`

---

## 목표

이후 모든 화면 이슈가 의존할 디자인 시스템 인프라(NativeWind 토큰, 다크모드 컨텍스트, 공통 레이아웃·바텀시트·광고 슬롯)를 셋업한다. `../design/`의 Vite 시안을 RN+NativeWind 환경으로 1:1 이식한다.

## 관련 파일

### 신규 생성

- `tailwind.config.js` — 시맨틱 토큰 등록
- `babel.config.js` — `nativewind/babel` preset
- `metro.config.js` — `withNativeWind`
- `global.css` — Tailwind 디렉티브
- `nativewind-env.d.ts` — NativeWind 타입
- `src/constants/colors.ts` — light/dark 팔레트
- `src/constants/icons.ts` — lucide 아이콘 사이즈 표준
- `src/contexts/ThemeContext.tsx` — `isDark` + 시스템 감지 + toggle
- `src/components/common/MobileLayout.tsx` — 헤더 + 바텀탭
- `src/components/common/BottomSheetModal.tsx` — `@gorhom/bottom-sheet` 래퍼
- `src/components/common/AdSlot.tsx` — placeholder

### 수정

- `app/_layout.tsx` — ThemeProvider + GestureHandlerRootView + BottomSheetModalProvider 연결, `global.css` import
- `app/index.tsx` — 데모 페이지 (MobileLayout + BottomSheet 데모 + AdSlot 3종)
- `tsconfig.json` — `@/*` → `src/*` 경로 alias 갱신
- `package.json` — 신규 의존성

### 정리 검토

- 루트 `components/`, `constants/`, `hooks/` (Expo 기본 템플릿 흔적) — 본 이슈에서는 새 `src/` 트리만 도입하고 기존 루트 폴더는 건드리지 않음 (별도 cleanup 이슈로 분리)

## 작업 분해

- [ ] 신규 의존성 설치: `nativewind`, `tailwindcss@3.4.x`, `@gorhom/bottom-sheet`, `lucide-react-native`, `react-native-svg`
- [ ] NativeWind 설정 (`tailwind.config.js` + `babel.config.js` + `metro.config.js` + `global.css` + `nativewind-env.d.ts`)
- [ ] `tsconfig.json` path alias `@/*` → `src/*`
- [ ] `src/constants/colors.ts` — DESIGN.md 색상 팔레트 (`light`, `dark` 객체)
- [ ] `src/constants/icons.ts` — 아이콘 사이즈 표준 (`ICON_SIZE.tab/header/card/caption`)
- [ ] `src/contexts/ThemeContext.tsx` — `isDark` + 시스템 감지(`useColorScheme`) + 수동 토글
- [ ] `src/components/common/MobileLayout.tsx` — Expo Router용으로 시안 이식 (`usePathname`, `useRouter`, `Pressable`, `SafeAreaView`)
- [ ] `src/components/common/BottomSheetModal.tsx` — handle + header + content 구조 + 다크모드
- [ ] `src/components/common/AdSlot.tsx` — `type: 'banner' | 'splash' | 'interstitial'` placeholder
- [ ] `app/_layout.tsx` — Provider 체인 + `global.css` import + Stack
- [ ] `app/index.tsx` — 인프라 데모 (MobileLayout 안에서 BottomSheet 토글 + AdSlot 3종)
- [ ] `expo lint` + `tsc --noEmit` 통과

## 완료 기준

- [ ] Expo 빌드 (`npm run start` 후 web/iOS 시뮬레이터 또는 lint+tsc) 성공
- [ ] Light/Dark 토글 동작 — 데모 페이지에서 ThemeContext.toggle 호출 시 색 전환
- [ ] `MobileLayout` 헤더·탭 정상 렌더 (Home 경로는 헤더 미노출, 탭 노출)
- [ ] `BottomSheetModal` 데모에서 열림/닫힘 + 다크모드 적용
- [ ] `AdSlot` 3종이 데모에서 placeholder로 렌더, 실 광고 SDK 호출 0
- [ ] `docs/DESIGN.md` 토큰 ↔ tailwind.config.js extend 매핑 일치

## 광고 정책

`docs/FRONTEND.md §7` 준수 — `AdSlot`은 회색 박스 + '광고 영역' 텍스트 placeholder만. 실 SDK 연동은 본 이슈 범위 밖.

## 시안 매핑 메모

- `../design/src/app/components/MobileLayout.tsx` → React Router `Outlet` 대신 Expo Router `Slot` 사용. `useNavigate/useLocation` → `useRouter/usePathname`. Web 전용(StatusBar 모킹, `max-w-md` 데스크탑 프레임)은 RN에서 제외. `SafeAreaView` 적용
- `../design/src/app/context/ThemeContext.tsx` → `useState` 단일에서 `useColorScheme`(시스템 감지) + 수동 오버라이드 우선순위로 확장
