# Exec Plan — Issue #5: Search 화면 (mock)

- **이슈**: https://github.com/When2Go/Mobile_When2Go/issues/5
- **라벨**: design, ui
- **PRD ID**: F-M01, F-M08, F-M09
- **브랜치**: `design/issue-search-screen#5`

## 목표

검색 화면(`app/search.tsx`) 전체를 placeholder → 실 구현으로 교체.
텍스트 입력·음성 버튼·최근 검색·검색 결과·Light/Dark 모드를 mock 데이터 기반으로 완성.

## 관련 파일

| 경로 | 상태 |
|------|------|
| `app/search.tsx` | 수정 (placeholder → 실 구현) |
| `src/components/search/SearchInput.tsx` | 신규 |
| `src/components/search/VoiceButton.tsx` | 신규 |
| `src/components/search/RecentSearchList.tsx` | 신규 |
| `src/components/search/SearchResultList.tsx` | 신규 |

## 작업 분해

- [x] 브랜치 생성 `design/issue-search-screen#5`
- [ ] `SearchInput` — 자동 포커스, 클리어 버튼, 아이콘
- [ ] `VoiceButton` — Mic 아이콘, 색상 토큰
- [ ] `RecentSearchList` — mock 7개, 개별/전체 삭제
- [ ] `SearchResultList` — mock 필터링, MapPin 아이콘
- [ ] `app/search.tsx` — 오케스트레이터, 음성 모달(Animated), 라우팅
- [ ] Light/Dark 모드 검증 (DESIGN.md 토큰 기준)

## 완료 기준

- [ ] Light / Dark 모드 모두 적용
- [ ] DESIGN.md 토큰 사용 (하드코딩 색 없음)
- [ ] 검색어 입력 → 결과 리스트 갱신 (mock 필터)
- [ ] 결과 탭 시 `/setup` 이동 + `destination` 파라미터 전달
- [ ] 음성 버튼 탭 → 모달 → listening → processing → `/setup` 자동 이동
- [ ] TypeScript 타입 에러 없음
