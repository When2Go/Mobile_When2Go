# 이슈 #7 — Result 화면 다중 경로 카드 + 예약 완료 팝업 (mock)

- 이슈: https://github.com/When2Go/Mobile_When2Go/issues/7
- Labels: `design`, `ui`
- PRD ID: F-M04(출발 시간 계산 — 결과 표시), F-M05(경로 선택), F-AD01(배너 광고 슬롯)
- 디자인 시안: `../design/src/app/pages/Result.tsx`

## 목표

Setup에서 입력한 정보를 바탕으로 계산된 mock 출발 시간 + 다중 경로 카드를 표시. 카드 탭 시 예약 확인 BottomSheetModal → 확인 → `/schedule`로 이동. F-AD01 배너 광고 placeholder 포함.

## 관련 파일

- `app/result.tsx` (신규) — Result 화면 라우트
- `src/components/result/DepartureTimeHeader.tsx` (신규) — 도착 시각 안내 + 안전 버퍼 표시
- `src/components/result/RouteCard.tsx` (신규) — 단일 경로 카드 (배지·시간·환승·스텝)
- `src/components/result/ReservationCompleteModal.tsx` (신규) — BottomSheetModal 기반 예약 확인 시트
- `src/constants/result.ts` (신규) — 경로 mock 데이터, 라벨, 안전 버퍼 mock 분
- `src/components/common/AdSlot.tsx` (재사용) — 배너 placeholder

## 작업 분해

- [ ] `src/constants/result.ts` — Route mock 타입/데이터, 라벨, 안전 버퍼 분 상수
- [ ] `src/components/result/DepartureTimeHeader.tsx` — "오후 2:30 도착을 위한 / 경로를 선택해 주세요"
- [ ] `src/components/result/RouteCard.tsx` — 배지(최적/최소환승/최소요금), 출발 시각, 소요/환승, 스텝 + lucide 아이콘
- [ ] `src/components/result/ReservationCompleteModal.tsx` — 선택 경로 요약 + 확인 → `/schedule`
- [ ] `app/result.tsx` — SafeAreaView + 헤더 + 카드 리스트 + 하단 AdSlot('banner') + 모달
- [ ] Light/Dark 모드 전 클래스 분기 확인
- [ ] PostToolUse Hook 통과 (ESLint/TypeScript)

## 완료 기준

- Light/Dark 모드 모두 적용
- NativeWind 시맨틱 토큰만 사용 (`#hex` 하드코딩 금지)
- 카드 탭 → 예약 확인 시트 → 확인 → `/schedule` 이동 mock 동작
- 안전 버퍼 mock값이 UI에 표시 (헤더에 "+10분 안전 버퍼" 등)
- `AdSlot type="banner"`가 빈 placeholder로만 렌더

## 작업 범위 밖

- 실 출발 시간 계산 / Trip API
- 예약 영구 저장
- 실 광고 SDK 연동
