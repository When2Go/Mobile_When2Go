# exec-plan: issue #87 — routeOption UI 필드 제거 및 API 요청값 TRANSIT 고정

- **이슈**: https://github.com/When2Go/Mobile_When2Go/issues/87
- **Labels**: refactor
- **PRD ID**: 없음

## 목표

setup.tsx · repeat.tsx의 경로 옵션 UI를 제거한다.
백엔드 API 스펙은 변경 없으므로 API 요청에는 routeOption 필드를 유지하되,
POST는 항상 `'TRANSIT'`, PUT은 항상 `'OPTIMAL'`로 고정한다.

## 관련 파일

| 파일 | 변경 내용 |
|---|---|
| `app/setup.tsx` | `routeType` 상태 및 `RouteOptionList` UI 제거 |
| `src/components/repeat/RepeatEditModal.tsx` | 경로 옵션 섹션 UI 제거 |
| `src/utils/reservationTransform.ts` | `routeOptionToApiOption` 이미 TRANSIT 고정 ✓ / `routeOptionToApiPutOption` 단순화 |

## 작업 분해

- [ ] `app/setup.tsx`: `routeType` state, `RouteOptionList` import·JSX 제거
- [ ] `src/components/repeat/RepeatEditModal.tsx`: 경로 옵션 섹션 JSX, 관련 import·변수 제거
- [ ] `src/utils/reservationTransform.ts`: `routeOptionToApiPutOption` 항상 `'OPTIMAL'` 반환으로 단순화

## 완료 기준

- [ ] 기능 동작 변화 없음 (경로 옵션 UI만 사라짐)
- [ ] ESLint + TypeScript 에러 없음
- [ ] POST API payload에 `routeOption: 'TRANSIT'` 포함 확인
- [ ] PUT API payload에 `routeOption: 'OPTIMAL'` 포함 확인
