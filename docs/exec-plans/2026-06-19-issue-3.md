# exec-plan: issue #3 — Splash 화면 + 앱 아이콘/네이티브 스플래시 등록

- **이슈**: https://github.com/When2Go/Mobile_When2Go/issues/3
- **제목**: [Design] Splash 화면 — TMAP 스타일 1~2초 브랜딩 + 작은 광고 슬롯 (mock)
- **Labels**: `design`, `ui`
- **PRD ID**: F-AD04 (스플래시 광고), F-AD02 통합 처리
- **브랜치**: `design/issue-splash-icon#3`

## 목표

세 갈래 작업을 함께 처리합니다.

1. **앱 아이콘 교체** — `appIcon.jpeg`(1254², 둥근 모서리+그림자 포함)를 풀블리드 정사각형으로 보정해 앱 아이콘으로 등록합니다.
2. **네이티브 스플래시 교체** — OS 런치스크린(`expo-splash-screen`)을 react 기본 로고에서 앱 아이콘 심볼로 교체합니다. (TMAP [Image #2] 패턴)
3. **인앱 스플래시 신규 구현** — 이슈 #3 본문대로 중앙 브랜딩 + 하단 `AdSlot` + 1.5초 후 자동 홈 이동 화면을 만듭니다.

## 사용자 확정 사항

- 아이콘 처리: **풀블리드 보정** (흰 배경으로 모서리/그림자 정리 → OS 자동 라운딩)
- 스플래시 범위: **네이티브 런치스크린 + 인앱 스플래시 둘 다**

## 작업 분해

### A. 이미지 자산 생성 (`magick` 사용)

- [ ] `appIcon.jpeg` → 카드 영역을 살짝 확대 크롭해 둥근 모서리/그림자 여백 제거 → 흰 배경 1024² PNG 풀블리드로 합성
- [ ] `assets/images/icon.png` 교체 (iOS/공통 아이콘)
- [ ] `assets/images/android-icon-foreground.png` 갱신 — 적응형 아이콘 안전영역(중앙 ~66%) 고려해 로고 스케일 조정
- [ ] `assets/images/android-icon-background.png` / `backgroundColor` 검토 — 현재 `#E6F4FE`, 아이콘 흰 배경과 톤 맞춤
- [ ] `assets/images/splash-icon.png` 교체 — 네이티브 스플래시용 심볼(흰 배경, `imageWidth: 200 contain`)
- [ ] `assets/images/favicon.png`(web) 갱신 — 선택

### B. `app.config.ts` 정리

- [ ] `expo-splash-screen` 플러그인: `backgroundColor: '#ffffff'` 유지, `dark.backgroundColor`는 라이트 통일(#58) 기조에 맞춰 흰색으로 통일 검토
- [ ] 아이콘 경로는 그대로(`icon.png`/`android-icon-*`) — 파일만 교체되므로 config 변경 최소

### C. 인앱 스플래시 화면 (이슈 #3 본문)

- [ ] `app/onboarding.tsx`(=splash 라우트) 생성: 중앙에 앱 아이콘/`지금 나가?` 브랜딩 + 하단 `<AdSlot type="splash" />`
- [ ] `constants/`에 `SPLASH_DURATION_MS = 1500` 선언 (숫자 리터럴 금지 규칙)
- [ ] 1.5초 후 `router.replace('/')` 자동 이동 (±100ms)
- [ ] 사용자 탭/스와이프 시 즉시 이동 (강제 대기 X)
- [ ] 하단 탭/헤더 숨김 처리
- [x] **라우팅 진입 방식 결정** — **루트 레이아웃 셸 게이트** 채택:
  - `app/_layout.tsx`에서 `showSplash` 상태로 콜드스타트 시 `<SplashView>`를 먼저 렌더, 종료 시 `<Stack>` 표시
  - 별도 onboarding 라우트로 보내면 홈(`/`, 지도 포함)이 먼저 마운트돼 깜빡임/성능 손해 → 셸 게이트가 더 견고
  - 이슈 본문이 "또는 `_layout`에서 처리할지 검토"로 열어둔 선택지. 미사용이 된 `MobileLayout`의 `/onboarding` 항목은 제거
  - 화면은 라우트 파일(`app/onboarding.tsx`) 대신 `src/components/splash/SplashView.tsx` 컴포넌트로 구현
- [ ] 다크모드 분기 신규 추가 금지(#58) — 라이트 톤으로만 구현

### D. 검증

- [ ] **네이티브 자산 변경 → 재빌드 필요** (아이콘/스플래시는 네이티브 리소스). `expo run:ios` 또는 EAS dev build (실기기 default — 사용자 명시 요청 시)
- [ ] 인앱 스플래시 동작은 Metro 재실행으로 확인 가능(JS-only)
- [ ] TDD 대상 점검: 자동 이동 타이머/즉시 이동 분기가 훅으로 분리되면 테스트 우선 작성

## 영향 파일

| 구분 | 파일 |
|---|---|
| 이미지 | `assets/images/icon.png`, `android-icon-foreground.png`, `splash-icon.png` (+ background/favicon 선택) |
| 설정 | `app.config.ts` (스플래시 backgroundColor 정리 정도) |
| 화면 | `app/onboarding.tsx`(신규), `app/_layout.tsx`(게이팅), `constants/*` |
| 재사용 | `src/components/common/AdSlot.tsx` (기존, 수정 없음) |

## 완료 기준

- [ ] 앱 아이콘이 풀블리드로 깔끔하게 등록(이중 라운딩/그림자 테두리 없음)
- [ ] 네이티브 런치스크린이 앱 심볼로 표시
- [ ] 인앱 스플래시: 1.5초 후 자동 홈 이동 + 탭 즉시 이동 + 하단 `AdSlot` placeholder
- [ ] 하드코딩 색상 0, 숫자 리터럴 상수화, 다크 분기 신규 추가 0
- [ ] 실기기에서 아이콘·스플래시·인앱 전환 확인

## 작업 범위 밖

- 실제 광고 SDK 연동 (심사 통과 후 별도 이슈, FRONTEND.md §7)
- 첫 실행 권한 요청 흐름 (별도 이슈)
