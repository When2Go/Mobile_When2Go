export const STORAGE_KEYS = {
  DEVICE_ID: '@when2go/device_id',
  RECENT_SEARCHES: '@when2go/recent_searches',
  REPEATS: '@when2go/repeats',
  ROUTES: '@when2go/routes',
  RESERVATION_TOGGLE: 'when2go.reservationToggle',
} as const;
