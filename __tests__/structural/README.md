# 구조적 테스트

`harness-feedback` Level 3 및 `/gc` 커맨드에서 자동 생성됩니다.

예시:
- 모든 화면 컴포넌트에 다크모드(`isDark`) 대응이 있는지 검증
- 하드코딩 색상값(`#`, `rgb(`, `rgba(`) 사용 여부 검증
- `@gorhom/bottom-sheet` 직접 import 사용 여부 검증
- `console.log` 잔존 여부 검증
